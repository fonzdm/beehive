apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: immich
resources:
  - repository.yaml
  - pvc.yaml
  - icloud-volume.yaml
  - secret.yaml
  - db.yaml
  # Disabled Helm release; manifest is retained as release.yaml.
  # - release.yaml
  - httproute.yaml