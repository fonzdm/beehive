apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nostr-relay-data
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: localpv-raid
  resources:
    requests:
      storage: 10Gi
