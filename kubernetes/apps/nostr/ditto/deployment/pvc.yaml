apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nostr-ditto-data
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: localpv-raid
  resources:
    requests:
      storage: 1Gi
