apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: lidarr-imagerepo
  namespace: flux-system
spec:
  image: ghcr.io/home-operations/lidarr
  interval: 5m

---

apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: lidarr-imagepolicy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: lidarr-imagerepo
  filterTags:
    pattern: '^rolling$'
  policy:
    alphabetical: {}
  digestReflectionPolicy: Always
  interval: 10m

---

