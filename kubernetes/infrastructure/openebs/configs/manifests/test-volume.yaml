apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-volume
  namespace: sopenebsr
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: localpv-raid