apiVersion: actions.summerwind.dev/v1alpha1
kind: RunnerDeployment
metadata:
  name: self-hosted-runner-deployment
  namespace: crd-ns
spec:
  template:
    spec:
      organization: humansbehindkeyboards
      labels:
        - self-hosted